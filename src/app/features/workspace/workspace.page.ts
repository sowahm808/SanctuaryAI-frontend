import { Component,input,computed,signal } from '@angular/core';
@Component({standalone:true,styles:[`.head{display:flex;justify-content:space-between;align-items:center}.layout{grid-template-columns:2fr 1fr}.item{display:flex;align-items:center;justify-content:space-between;padding:1rem 0;border-bottom:1px solid var(--line)}@media(max-width:800px){.layout{grid-template-columns:1fr}}`],template:`<header class="head"><div><p class="eyebrow">Ministry workspace</p><h1>{{title()}}</h1><p class="muted">Create, organize, review, and publish your church's {{title().toLowerCase()}}.</p></div><button class="btn" (click)="create()">+ Create</button></header><div class="grid layout"><section class="card"><h2>Active work</h2>@for(i of items();track i){<div class="item"><span><b>{{i}}</b><br><small class="muted">Updated today · Grace Community Church</small></span><span class="badge">Draft</span></div>}@empty{<div class="empty"><h3>Nothing here yet</h3><p>Create your first item to start the ministry workflow.</p><button class="btn" (click)="create()">Create now</button></div>}</section><aside class="card"><h2>Workflow</h2><p><span class="badge">1</span> Create and save a draft</p><p><span class="badge">2</span> Collaborate and review</p><p><span class="badge">3</span> Approve and publish</p></aside></div>`}) export class WorkspacePage{readonly kind=input('content');readonly title=computed(()=>this.kind().split('-').map(x=>x[0]?.toUpperCase()+x.slice(1)).join(' '));readonly items=signal<string[]>([]);create(){this.items.update(x=>[`Untitled ${this.title()}`, ...x])}}
