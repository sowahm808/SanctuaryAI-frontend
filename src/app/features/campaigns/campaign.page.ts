import { Component,signal } from '@angular/core';import { FormControl,FormGroup,ReactiveFormsModule,Validators } from '@angular/forms';import type { CampaignSection } from '../../models/domain.models';
@Component({standalone:true,imports:[ReactiveFormsModule],styles:[`.split{grid-template-columns:340px 1fr}.sections{grid-template-columns:repeat(2,1fr)}.section{display:flex;gap:1rem;align-items:center}.ring{width:44px;height:44px;border-radius:50%;display:grid;place-items:center;background:#eee9fb;color:var(--violet);font-weight:800}.form{display:grid;gap:1rem}.toolbar{display:flex;justify-content:space-between;gap:1rem;align-items:center}.section .btn{margin-left:auto}@media(max-width:900px){.split,.sections{grid-template-columns:1fr}}`],template:`<div class="toolbar"><div><p class="eyebrow">Campaign builder</p><h1>Build a monthly ministry plan</h1><p class="muted">Generate each section asynchronously, review it, then lock approved work.</p></div><button class="btn" (click)="generateAll()">✦ Generate all</button></div><div class="grid split"><form class="card form" [formGroup]="form"><h2>Campaign brief</h2><div class="field"><label>Month</label><input type="month" formControlName="month"></div><div class="field"><label>Spiritual focus</label><input formControlName="focus" placeholder="e.g. Kingdom authority"></div><div class="field"><label>Main scripture</label><input formControlName="scripture" placeholder="Book chapter:verse"></div><div class="field"><label>Preferred tone</label><select formControlName="tone"><option>Pastoral</option><option>Prophetic</option><option>Teaching</option></select></div><div class="field"><label>Prayer point quantity</label><input type="number" min="1" max="50" formControlName="quantity"></div><button type="button" class="btn secondary">Save draft</button></form><section><h2>Campaign completion</h2><div class="grid sections">@for(s of sections();track s.id){<article class="card section"><div class="ring">{{s.progress}}%</div><div><b>{{s.title}}</b><br><span class="muted">{{s.status}}</span></div><button class="btn secondary" [disabled]="s.locked" (click)="generate(s.id)">{{s.progress?'Open':'Generate'}}</button></article>}</div></section></div>`}) export class CampaignPage{readonly form=new FormGroup({month:new FormControl('2026-08',{nonNullable:true,validators:[Validators.required]}),focus:new FormControl('',{nonNullable:true,validators:[Validators.required]}),scripture:new FormControl('',{nonNullable:true,validators:[Validators.required]}),tone:new FormControl('Pastoral',{nonNullable:true}),quantity:new FormControl(20,{nonNullable:true})});readonly sections=signal<CampaignSection[]>(['Theme of the Month','Pastoral introduction','Spiritual objectives','Monthly declaration','Prayer collection','Weekly sermon series','Weekly flyer plan','Social media plan','Short-form video plan','Publishing calendar'].map((title,index)=>({id:String(index),title,progress:index<2?100:0,status:index<2?'Approved':'Draft',locked:index<2})));generate(id:string){this.sections.update(items=>items.map(s=>s.id===id?{...s,progress:100,status:'Awaiting Approval'}:s))}generateAll(){this.sections.update(items=>items.map(s=>s.locked?s:{...s,progress:100,status:'Awaiting Approval'}))}}
