import type { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig = {
  appId: "church.sanctuary.ai",
  appName: "SanctuaryAI",
  webDir: "dist/sanctuary-ai/browser",
};
export default config;
